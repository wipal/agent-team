# Variant: Go Gin - Backend Developer

## Tech Stack
- **Framework:** Gin (Go web framework)
- **Language:** Go 1.21+
- **Architecture:** Clean Architecture / Hexagonal
- **Database:** PostgreSQL / MySQL
- **Validation:** go-playground/validator
- **Testing:** Go testing + testify

## Project Structure
```
cmd/
├── api/
│   └── main.go           # Entry point
internal/
├── handler/              # HTTP handlers (controllers)
│   ├── user_handler.go
│   └── auth_handler.go
├── service/              # Business logic
│   ├── user_service.go
│   └── auth_service.go
├── repository/           # Data access
│   ├── user_repository.go
│   └── postgres/
├── model/                # Domain models
│   ├── user.go
│   └── dto/
├── middleware/           # HTTP middleware
│   ├── auth.go
│   └── cors.go
├── config/               # Configuration
│   └── config.go
└── pkg/                  # Shared utilities
    ├── response/
    └── validator/
pkg/                      # Public packages
└── utils/
```

## Handler Pattern
```go
// internal/handler/user_handler.go
package handler

import (
    "net/http"
    "github.com/gin-gonic/gin"
    "myapp/internal/service"
    "myapp/internal/model/dto"
)

type UserHandler struct {
    userService service.UserService
}

func NewUserHandler(us service.UserService) *UserHandler {
    return &UserHandler{userService: us}
}

func (h *UserHandler) RegisterRoutes(r *gin.RouterGroup) {
    users := r.Group("/users")
    {
        users.GET("", h.List)
        users.GET("/:id", h.GetByID)
        users.POST("", h.Create)
        users.PUT("/:id", h.Update)
        users.DELETE("/:id", h.Delete)
    }
}

func (h *UserHandler) GetByID(c *gin.Context) {
    id := c.Param("id")

    user, err := h.userService.GetByID(c.Request.Context(), id)
    if err != nil {
        c.JSON(http.StatusNotFound, gin.H{"error": "user not found"})
        return
    }

    c.JSON(http.StatusOK, gin.H{"data": user})
}
```

## Service Pattern
```go
// internal/service/user_service.go
package service

import (
    "context"
    "myapp/internal/repository"
    "myapp/internal/model"
)

type UserService interface {
    GetByID(ctx context.Context, id string) (*model.User, error)
    Create(ctx context.Context, req *dto.CreateUserRequest) (*model.User, error)
}

type userService struct {
    repo repository.UserRepository
}

func NewUserService(r repository.UserRepository) UserService {
    return &userService{repo: r}
}

func (s *userService) GetByID(ctx context.Context, id string) (*model.User, error) {
    return s.repo.FindByID(ctx, id)
}
```

## Middleware Pattern
```go
// internal/middleware/auth.go
package middleware

import (
    "net/http"
    "strings"
    "github.com/gin-gonic/gin"
    "myapp/internal/pkg/jwt"
)

func AuthRequired() gin.HandlerFunc {
    return func(c *gin.Context) {
        authHeader := c.GetHeader("Authorization")
        if authHeader == "" {
            c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "missing authorization"})
            return
        }

        tokenString := strings.TrimPrefix(authHeader, "Bearer ")
        claims, err := jwt.ValidateToken(tokenString)
        if err != nil {
            c.AbortWithStatusJSON(http.StatusUnauthorized, gin.H{"error": "invalid token"})
            return
        }

        c.Set("userID", claims.UserID)
        c.Next()
    }
}
```

## Error Response Format
```go
// Standard error response
c.JSON(http.StatusBadRequest, gin.H{
    "error": gin.H{
        "code": "VALIDATION_ERROR",
        "message": "Validation failed",
        "details": []gin.H{
            {"field": "email", "message": "invalid email format"},
        },
    },
})
```

## Testing Pattern
```go
// internal/handler/user_handler_test.go
package handler

import (
    "bytes"
    "encoding/json"
    "net/http"
    "net/http/httptest"
    "testing"
    "github.com/gin-gonic/gin"
    "github.com/stretchr/testify/assert"
    "github.com/stretchr/testify/mock"
)

func TestGetByID(t *testing.T) {
    gin.SetMode(gin.TestMode)

    // Setup
    mockService := new(MockUserService)
    handler := NewUserHandler(mockService)

    mockService.On("GetByID", mock.Anything, "1").Return(&model.User{
        ID: "1", Name: "Test User",
    }, nil)

    // Create request
    w := httptest.NewRecorder()
    c, _ := gin.CreateTestContext(w)
    c.Params = gin.Params{{Key: "id", Value: "1"}}

    // Execute
    handler.GetByID(c)

    // Assert
    assert.Equal(t, http.StatusOK, w.Code)
    mockService.AssertExpectations(t)
}
```

## Key Conventions

### Naming
- Files: `snake_case.go`
- Packages: `lowercase` (no underscores)
- Interfaces: `noun` or `verb + er` (e.g., `UserService`, `Reader`)
- Structs: `PascalCase`
- Functions: `PascalCase` (exported) or `camelCase` (private)

### Error Handling
```go
// Always wrap errors with context
if err != nil {
    return fmt.Errorf("failed to get user: %w", err)
}

// Use custom errors for domain logic
var ErrUserNotFound = errors.New("user not found")
```

### Validation
```go
type CreateUserRequest struct {
    Email    string `json:"email" validate:"required,email"`
    Password string `json:"password" validate:"required,min=8"`
    Name     string `json:"name" validate:"required,min=2,max=100"`
}
```

## Skills Reference
- `domain/backend/api-design` - RESTful patterns
- `domain/backend/database-design` - PostgreSQL/MySQL
- `domain/backend/security` - Auth, JWT
- `domain/backend/testing-be` - Go testing
